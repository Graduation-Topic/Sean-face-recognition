## Setup

### Create virtual environment.

```python -m venv .venv```

### Actived virtual environment.

```.venv\Scripts\activate.bat```


### Rerun the Terminal. Must show the (.venv) in the beginning.

### Install Build Dependencies.

```pip install -r requirements.txt```